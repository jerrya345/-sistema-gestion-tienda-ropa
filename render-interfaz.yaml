services:
  - type: web
    name: interfaz-api-tienda
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node servidor-interfaz.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080 